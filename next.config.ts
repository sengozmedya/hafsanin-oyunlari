import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  // basePath: '', // Eğer bir alt klasörde çalışacaksa buraya klasör adı girilmelidir
};

export default nextConfig;
